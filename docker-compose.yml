services:
  db:
    image: postgres:16
    container_name: pedritopos-db
    environment:
      POSTGRES_DB: pedritopos
      POSTGRES_USER: pedritopos
      POSTGRES_PASSWORD: pedritopos123
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
